            <div class="code-wrap">
                <div class="code-label">
                    <div class="code-lang-row"><div class="code-dots"><span></span><span></span><span></span></div><span class="code-lang">go</span></div>
                    <button class="copy-btn" onclick="copyCode(this)"><i class="fa-regular fa-copy"></i> copy</button>
                </div>
                <pre><code class="language-go">package main

import (
    "fmt"
    ctoon "github.com/mohammadraziei/ctoon"
)

func main() {
    data := map[string]interface{}{
        "name": "Alice",
        "age":  int64(30),
    }

    // Encode → TOON
    toon, _ := ctoon.Dumps(data)
    fmt.Println(toon)

    // Decode ← TOON
    val, _ := ctoon.Loads(toon)
    fmt.Println(val)

    // File I/O
    ctoon.EncodeToFile(data, "out.toon", ctoon.DefaultEncodeOptions())
    val, _ = ctoon.DecodeFromFile("out.toon", ctoon.DefaultDecodeOptions())

    // JSON interop
    json, _ := ctoon.DumpsJSON(data, 2)
    fmt.Println(json)
    ctoon.DumpJSON(data, "out.json")
}</code></pre>
            </div>
