            <div class="code-wrap">
                <div class="code-label">
                    <div class="code-lang-row"><div class="code-dots"><span></span><span></span><span></span></div><span class="code-lang">rust</span></div>
                    <button class="copy-btn" onclick="copyCode(this)"><i class="fa-regular fa-copy"></i> copy</button>
                </div>
                <pre><code class="language-rust">fn main() -&gt; ctoon::Result&lt;()&gt; {
    // Decode ← TOON
    let val = ctoon::loads("name: Alice\nage: 30")?;
    println!("{}", val["name"].as_str().unwrap()); // Alice

    // Encode → TOON
    let toon = ctoon::dumps(&amp;val)?;
    println!("{toon}");

    // JSON interop
    let json = ctoon::dumps_json(&amp;val, 2)?;
    let back = ctoon::loads_json(&amp;json)?;
    println!("{}", back["age"].as_u64().unwrap()); // 30

    Ok(())
}</code></pre>
            </div>
