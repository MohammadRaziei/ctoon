<div class="code-wrap">
    <div class="code-label">
        <div class="code-lang-row"><div class="code-dots"><span></span><span></span><span></span></div><span class="code-lang">julia</span></div>
        <button class="copy-btn" onclick="copyCode(this)"><i class="fa-regular fa-copy"></i> copy</button>
    </div>
    <pre><code class="language-julia">using CToon

# Parse JSON → native Julia data
data = CToon.parse("""{"name": "Alice", "age": 30}""")
println(data["name"])   # Alice
println(data["age"])    # 30

# Objects decode as order-preserving OrderedDict{String,Any},
# arrays as Vector{Any}
nested = CToon.parse("""{"items": [1, 2, 3]}""")
println(nested["items"])   # Any[1, 2, 3]</code></pre>
</div>
